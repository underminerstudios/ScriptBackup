## tools requirements on Mac OS X

First, install Homebrew

```
$ brew install imagemagick jpegoptim pngquant
$ brew install ffmpeg --with-libvorbis
$ sudo easy_install pip
$ pip install requests lxml
```

Enjoy.
